# Backend (Express API)
PORT=3001
NODE_ENV=development
JWT_SECRET=change_me_dev_secret
DATABASE_URL=mysql://root:root@localhost:3306/nurvio_hub

# Admin accounts for seeding (optional; seed script will use defaults if allowed)
ADMIN1_EMAIL=orange.admin@nurvio.de
ADMIN1_PW=Root.Orange!
ADMIN2_EMAIL=vez.admin@nurvio.de
ADMIN2_PW=Root.Vez!

# SMTP (optional)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Nurvio Hub <no-reply@localhost>"

# Frontend
VITE_API_BASE=http://localhost:3001